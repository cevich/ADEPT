---

peon_groups:
    - "{{ hostvars.kommandir.peon_cloud_group | mandatory }}"
    - fedoratomic
    - updated
    - docker
    # overlay2 - already the default
    - autotested

peon_image: 'Fedora-Atomic-26'
