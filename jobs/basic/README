This job ({{ job_name }}) is intended to exercise
a small subset of tests against an all-defaults,
vanilla set of peons to verify basic functionality.
The set of tests is specified in the jobs
``kommandir_vars.yml`` file.

(src: {{ job_path }}/{{ job_docs_template_src }})
